import * as env from "./env";

export const ROUTE_NOTES = `${env.REACT_APP_API_BASE}/notes`;
export const TIME_FORMAT = {
  hour: "2-digit",
  minute: "2-digit",
  year: "numeric",
  month: "short",
  day: "numeric",
};
